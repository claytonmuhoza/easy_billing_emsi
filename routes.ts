/** route to do login
 * @type {string[]}*/
export const publicRoutes = ['/', '/api/autosend']
/** prefixe route to api auth
 * @type {string}*/
export const apiAuthPrefix = '/api/auth'
/** page where to redirect when login
 * @type {string}
 */
export const DEFAULT_Login_REDIRECT = '/home'
