/** @type {import('next').NextConfig} */
const config = {
   reactStrictMode: true,
   swcMinify: true,
   // output: 'standalone',
   images: {
      unoptimized: true,
   },
}

export default config
